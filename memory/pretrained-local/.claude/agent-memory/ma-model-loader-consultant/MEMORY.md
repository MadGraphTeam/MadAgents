## Slice
I own `import model` orchestration: `do_import` branching, gauge selection/auto-switch, CMS activation, aloha_prefix choice, V4-vs-UFO, UFOError auto-convert recovery, process_model + add_default_multiparticles. NOT mine: UFO model content + pickle cache (ufo slice), restrict_*.dat algorithm (restriction slice), generate/define syntax (process-syntax slice), compute_widths (madwidth slice), online model DB fetch (installation slice).

## Core operating principles
- Source = code AND config/data files MG reads (multiparticles_default.txt, particles_name_default.txt, model __init__ gauge bits). Verify for THIS input.
- Default mode: a scope-matching cached page counts as verification — adopt it, sanity-check one cited file:line. Under mg-deep-verify, walk source every time.
- Reject unmarked out-of-slice claims; answer in-slice portion only; name owning slice only if a listed redirect.
- Two-section return: Source-walked facts (file:line + quotes) then Implications (DIRECT/INFERRED/HYPOTHESIS). Concision rule applies.
- Pretrained MadGraph recall is hypothesis, not evidence — confirm against source.

## Recent lessons (FIFO, max 5)
- return-doesnt-mean-skipped: a bare `return` in do_import's gauge auto-switch (`:5824/:5829/:5837`) does NOT leave setup undone — the preceding `do_set('gauge X')` makes set2_gauge self-re-import via `do_import(...force=True)` (`set2_gauge:8159`), which runs the full tail (process_model). Trigger: claiming "X is skipped because of an early return." Correct: trace whether a called helper completes the work elsewhere before asserting it's skipped. (probe: `import model sm; set gauge Feynman` re-fires name-pass + "Kept multiparticles".)
- gauge-truthiness-not-non-unitary: `aloha.unitary_gauge` is True/1=unitary, 2=axial, 3=FD, False=Feynman — axial(2)/FD(3) are TRUTHY. A `not aloha.unitary_gauge` branch (e.g. massive-vector prop=0 at import_ufo.py:1325) fires for **Feynman ONLY**, not "non-unitary" broadly; `if aloha.unitary_gauge` (prop-list pick :1298) treats axial/FD as unitary-like. Trigger: writing "under non-unitary" for a code site. Correct: name the exact predicate (`in [1,2]` vs `if` vs `not` vs `==N`) and check axial/FD per the truthiness table on gauge-dependent-model-loading. (probe: SM W/Z prop=`''` under unitary/axial/FD, `0` only under Feynman.)
- installed-model-set-volatile: a MINIMAL MG5_aMC install ships only `sm`, `loop_sm`, `MSSM_SLHA2` (+ plugin/aux `hgg_plugin`, `taudecay_UFO`) under models/. `heft` does NOT ship; `sm-no_b_mass` is a RESTRICTION of sm, not a dir; 2HDM*/SMEFTatNLO/dim6top/loop_qcd_qed_sm* are online-fetched on demand and may be ABSENT. Trigger: asserting a model is "installed" or reciting a model-membership list. Correct: `for d in "$MADGRAPH_INSTALL"/models/*/; do [ -f "$d/particles.py" ] && basename "$d"; done`. My gauge/do-import pages' 2HDM/SMEFTatNLO probe claims hold ONLY where those are actually present.
- addmodel-vertex-dedup-by-order-not-value: `add model PLUGIN` merge (usermod.add_model) dedups vertices by (sorted-PDG + lorentz + color + coupling ORDER), NOT coupling expression — a plugin vertex matching an existing one on those is SKIPPED even if its coupling VALUE differs (warning "Did NOT add interaction ..."; usermod.py:906-919). "near-duplicates both kept" is WRONG. Also: plugin can't be a loop model (:982); external params merge by block+lhacode with NO value check (:696-709); coupling orders take MIN of hierarchy AND expansion_order (:802-811); particle mass-merge is zero-yields-to-nonzero ELSE raise, not "nonzero wins" (:633-644).
- cms-is-set-option-not-runcard: `complex_mass_scheme` is a GLOBAL MG5 interface option (default False, madgraph_interface.py:3105), toggled ONLY by `set complex_mass_scheme True` (set2 :8025-8054) — NOT a run_card param and NOT auto-on for NLO (grep for a forcing assignment is empty; CMS is explicit opt-in for BOTH LO and NLO). Applied at import via do_import:5789 → rewrites masses to complex at LO too. The ONLY file it's written to is proc_characteristics (banner.py:1761, ProcCharacteristic class, NOT RunCardLO@4187/RunCardNLO@5594); read back at launch by common_run_interface.py:263. set2 with a model loaded self-re-imports via do_import(...{'allow_qed_cms':True}) (:8054). CMS is orthogonal to the Lorentz gauge (only touches the EW input scheme). Trigger: any "CMS run_card param / auto-on for NLO" claim → reject.

## Wiki page index
- import-time-model-rewrites: PRINCIPLE page — operative model after `import model` ≠ on-disk UFO + card literal; FOUR knobs silently rewrite it at load: CMS (EW-scheme flip), restriction+multiparticle (j/p flavour scheme), gauge (Goldstones/pickle), mdl_ prefix (param-name rename, breaks `set cp`). Route "my model isn't what I wrote" surprises here, then to the owning mechanism page; tell user to `display`.
- do-import-model-flow: do_import model dispatch — check_import pre-branch gate (flags, --last, find_import_type guess), find_ufo_path resolution order + online-fetch gate, get_path_restrict restrict-as-string + `SMEFTatNLO-NLO` suffix-split worked example + bare-name restrict_default auto-pick, V4 deprecation, prefix, UFO load with CMS wrapper (3-arm keep_external SLHA2, double param_card read), post-load gauge check, process_model; import_ufo_model convenience method + non-model tails.
- gauge-selection-and-loopmodel-autoswitch: gauge-bit selection, loop_qcd_qed_sm Feynman force (NOT installed locally), LoopModel `perturbative_expansion>0` predicate (import_ufo:498-510, consumer's-own-test). MECHANISM (not a count): QCD-only ['QCD'] LoopModel lands in [[],['QCD']] exclusion → stays unitary, no Feynman force; only NON-QCD pert (QED/EW) forces. Membership is install-volatile — page carries a LIVE-SCAN recipe (python3 over coupling_orders.py, perturbative_expansion>0), don't quote the count. Last-scan set: loop_sm/SMEFTatNLO/2HDM5F_NLO/2HDMtII_NLO/2HDMtypeII (re-confirm). + bare-name restrict gated SOLELY on restrict_default.dat + import-switches-GAUGE-ONLY-never-INTERFACE boundary, gauge=[0,1], set2_gauge->aloha.unitary_gauge mapping.
- gauge-dependent-model-loading: converter interior — aloha.unitary_gauge drives Goldstone drop/keep (:1245), gauge-keyed propagator element pick (:1295), massive-vector propagator=0 (:1325), FD merge_all_goldstone (:608); gauge/prefix-keyed pickle cache + _import_once modified-on-disk guard; non-QCD-gluon disables pickle.
- ufoerror-autoconvert-recovery: UFOError catch -> auto_convert_model retry via 'convert model' or InvalidCmd guidance when off; do_convert_model confirmation prompt silently skipped on auto-convert path.
- process-model-and-multiparticle-defaults: process_model autocomplete build + add_default_multiparticles loading, j/p 4/5-flavour scheme + photon adjustment after restriction. Default p/j carry NO photon (file literal g u c d s + antiquarks; photon added only if perturbation_couplings has 'QED') → rules out γγ/photon-PDF story for default-setup σ. LEPTON labels: l+/l- = e/mu ONLY (NO tau), vl/vl~ = 3-gen INCL vt (tau asymmetry); l±/vl get no post-processing (file literal). Default massive-b sm p/j = 4-flav by FILE LITERAL+no-op (scheme stays "old", NO log); active scheme=4 removal only if 5-flav def pre-existed; scheme=5 ADD fires only massless-b (sm-no_b_mass). Name-split sm-no_b_mass = get_path_restrict:210-220 after UFOImportError. Plus custom-UFO directory-path load (import model ./HVT) via find_ufo_path.
- cms-activation-interior: CMS activation SURFACE (global `set complex_mass_scheme` option, NOT run_card param, NOT NLO-auto-on, applied at LO too via do_import:5789, recorded in proc_characteristics banner.py:1761, orthogonal to Lorentz gauge) + change_mass_to_complex_scheme interior — CMSParam flip, CMASS_ synthesis, SM W-mass EW-scheme auto-switch to (mz,mw,alpha), Yukawa-to-mass tie, full complexification; bypass_check/allow_qed_cms dead in v3.7.1 EW path.
- ufo-to-internal-object-conversion: UFOMG5Converter interior — UFO particle/vertex → base_objects.Particle/Interaction; attr mapping + nb_property==10, incoming/outcoming UFO-pair dedup, lazy antiparticle deep-copy at particle_dict build (list=half dict), self_antipart, color 3/3bar rep detect, fermion-flow sign, conservecharge prune, mass/width-as-param-name-string, params/couplings via OrganizeModelExpression; no reachability-prune here (that's restriction).
- add-model-plugin-merge: `add model PLUGIN` orchestration (do_add:3260 -> add_model:3400) + usermod.UFOModel.add_model merge engine. BASE__PLUGIN merged-dir cache (reused unless --recreate/--output); merge rules: loop-model plugin PROHIBITED (:982), coupling-order MIN of hierarchy AND expansion_order (:802-811), external-param block+lhacode merge NO value check (:696-709), particle mass zero-yields-to-nonzero ELSE raise (:633-644), vertex dedup by particles+lorentz+color+ORDER not coupling value (near-dup SKIPPED w/ warning, :906-919), collision-rename suffix `__1` (NOT plugin name). Re-imports merged dir -> full import-time rewrites reapply.
- mdl-prefix-parameter-naming: the mdl_ prefix RENAMES model params (change_parameter_name_with_prefix base_objects.py:1627, NOT aloha_prefix) at import; rename exclusion list as/mu_r/zero/aewm1/g (5, :1658) — DISTINCT from the pickle-check's 4-element as/mu_r/zero/aewm1 (:382, no g); param_card keyed by lhablock+lhacode and STRIPS mdl_ from comment (write_param_card.py:246-247, reconciled w/ ufo) → prefix toggle never breaks card matching/bytes (corrects the naive caution; breakage is names-in-CODE only); EFT Wilson coeffs → mdl_cX.
