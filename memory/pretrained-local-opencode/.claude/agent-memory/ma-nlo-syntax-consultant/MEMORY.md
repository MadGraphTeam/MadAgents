## Slice
I own the NLO `[…]` perturbation-coupling bracket syntax in `extract_process`: how `[QCD]`,
`[virt=]`, `[real=]`, `[noborn=]`, `[sqrvirt=]`, `[LOonly=]`, `[only=]` parse; what `LoopOption`/
`HasBorn` they set; `_valid_nlo_modes`; the parse-time gauge restriction; `'has_born'` propagation.
Out of slice: MadLoop runtime, FKS algorithm, NLO Fortran export, aMC@NLO runtime, loop-model UFO
requirements, tree-level coupling-order syntax (`QED=`/`^2`/`WEIGHTED`), diagram filters, decay/polarization parsers.

## Core operating principles
- Verify against source for THIS input every dispatch; adopt a scope-matching cached page per ma-wiki-as-evidence (sanity-check one file:line), else walk source.
- Source = code + config files together; read the actual file, never recall.
- Runtime predictions (error text, NLO_mode value, has_born) need a parse/launch probe before being cached as fact; else mark hypothesis.
- Judge only inside the slice; treat marked premises as true, reject unmarked out-of-slice claims, hand off questions whose answer lies outside the slice.
- Two-section return (Source-walked facts / Implications); concision rule applies.

## Recent lessons (FIFO, max 5)
- inner-extract_process-guards-shadowed-live: NEVER report an extract_process bracket-guard message (4871 invalid-mode, 5269-5273 order_hierarchy, 5283/5286 loop-model) as "what the user sees" without checking the LIVE path — these INNER guards are shadowed on every `generate`/`add` by EARLIER outer guards: Switcher _valid_nlo_modes (268/211) for a bad OPTION, aMC@NLO do_add validate_model (513) for a non-perturbable ORDER (which can also AUTO-UPGRADE sm->loop_sm instead of rejecting). The inner message appears ONLY on a direct extract_process call. This shipped wrong in 3 pages ([NP]/[QED]->5286, [EW]->order_hierarchy, [bogus=]->4871) — all corrected. Probe end-to-end AND isolation; they differ. Probe-confirmed (NP/QED/EW/bogus/sm). Page extract-process-guards-shadowed-by-outer-routing.
- qed0-omission-is-contamination-not-loop-induced: NEVER repeat "omitting QED=0 in SMEFTatNLO zeroes QCD -> loop-induced" as the shipped-default mechanism — WRONG. Unspecified coupling orders fill to `default_unset_couplings` which ships an unbounded "infinity" sentinel, NOT 0 (read default fresh at madgraph_interface:3112); the amcatnlo fill loop (amcatnlo_interface:533-537) fires only when orders is NON-empty and fills missing orders to that sentinel. So `p p > t t~ NP=2 [QCD]` (QED omitted) sets QED AND QCD to the unbounded sentinel -> EW Born ADMITTED = contamination, NOT loop-induced; QCD is never 0 so qq̄/gg Born always present. "QCD->0->loop-induced" needs `set default_unset_couplings 0` (non-default override, example at 8185). Bare `[QCD]` (NO orders at all) skips the fill loop -> auto-order block (545-561) gives qed=-1 -> ERRORS at neg-constraint guard (559), never silently defaults. QED=0 excludes EW Born; an explicit QCD=N pins the QCD Born power (N = count of QCD vertices in the Born tree, DERIVE per process, never a fixed integer); both load-bearing. Source-decisive (fill-to-sentinel-not-0); Born-topology outcome is a probe-candidate. Page np-order-with-bracket-smeftatnlo (QED=0 section).
- two-guards-disjoint-coverage-NOT-shadowing: NEVER mark a guard DEAD/SHADOWED from REASONING ("an earlier guard catches the same case") without probing the EXACT input each guard's predicate matches — they may cover DISJOINT forms. The single-$ NLO guards: check_add:129 (`'$' in args`) fires only on SPACED `$ g` (standalone token); do_add:476 regex (`\b\$\b`) fires only on GLUED `d$g` (word-char BOTH sides) — neither shadows the other, BOTH LIVE; `~$g` and `$$` hit NEITHER. I had shipped do_add:476 as "shadowed/dead" — WRONG, probe `generate u u~ > d d~ g d$g [QCD]` hits 476. This is the dead-code-liveness trap in its costliest INVERSE direction (a reachable guard marked dead). When two guards target "the same syntax," tabulate predicate-vs-input-form and probe each form. Probe-confirmed. Page nlo-parse-guard-firing-order.
- nlo-mode-is-a-4-stage-lifecycle: NEVER answer "what NLO_mode/has_born does process X end up with" from the typed bracket alone — the value passes through 4 stages (routing classify extract_process_type 162 -> parse remap 4862-4877 -> stored 5350 -> command override do_check 4414 / create_loop_induced 5392). Identify WHICH stage the question asks about, then take the LAST stage that fires. Stages 1 and 2 disagree by NAME (sqrvirt/LOonly surface vs remapped). This is the broad rule the two-parsers / post-remap-discriminant / mutated-twice lessons are each one stage of. Full page nlo-mode-lifecycle-stages.
- explicit-noborn-bypasses-C6a-squared-order-reject: NEVER say "^2==/^2> squared orders are rejected at NLO for ALL bracket processes" — the C6a `<=`-only guard (amcatnlo:541-542) lives ONLY in aMC@NLO do_add. Explicit `[noborn=QCD]` routes Switcher master:218-225 STRAIGHT to create_loop_induced (RETURN at 225), bypassing aMC@NLO do_add — create_loop_induced has NO sqorders_types check, so `^2==`/`^2>` ARE ACCEPTED for explicit `[noborn=]` (probe: `g g > z z QED^2==4 [noborn=QCD]` OK / 28 loops) where the SAME order on `[QCD]` is REJECTED at 542. Predicate = TYPED bracket (nlo_mode=='noborn'), NOT physical loop-inducedness: a gg-only physically-loop-induced process typed `[QCD]` still routes aMC@NLO and hits 541 BEFORE NoBornException. I had over-generalized C6a as "fires on ANY [...] incl loop-induced" — corrected on np-order + create-loop-induced pages. Coupling-order NAME must be a model order (QED^2 ok; `HZZ^2` fails at parse 4929-4932). noborn is procdef-GLOBAL (drops genuine Born, probe b b~>h: 0 born vs [QCD] 1 born). has_born-mix reject is ORDER-DEPENDENT (5408 LO-then-noborn / Switcher combine-LO/NLO for noborn-then-[QCD] / FKS-first ESCAPES). Decay+^2==N -> 3301-3303 "Decay processes cannot specify squared orders constraints". All probe-confirmed loop_sm.

## Wiki page index
- bracket-parse-and-loopoption-mapping: [...] parse in extract_process; option= -> LoopOption/HasBorn map; parallel order-keyword path ([all]/[loonly] expand to full pert set); only mode parse-accepted/unhandled; bracket pertOrders NOT coupling-aliased ([EW] fails where EW=2 works); _valid_nlo_modes; help text under-documents accepted modes; lands on ProcessDefinition. ALSO @N stripped at 4844 BEFORE bracket regex 4852 -> `[QCD] @N` coexist orthogonally; FxFx multi-mult = [QCD] on EVERY line (each independent full-NLO procdef, @0 no-suffix/@1+ print), auto born-sqorders use <= (probed loop_sm p p>t t~ [+j]).
- gauge-restriction-and-pert-order-validation: Parse-time guards after the bracket parse — FD/axial gauge restriction for loop processes, loop-model requirement, tagged-particle rejection, pert-order validation.
- loopoption-is-the-post-remap-discriminant: LoopOption (post-remap), not the surface option= keyword, is what every post-parse guard branches on; keyword consumed at the chokepoint, only HasBorn survives.
- switcher-extract-process-type-predispatch: The Switcher (master_interface) parses the bracket TWICE — extract_process_type (162) classifies nlo_mode and routes generate/add/check to MadGraph/MadLoop/aMC@NLO BEFORE extract_process runs; own LOonly->QCD default, no option-validity check, virtsqr typo in do_generate, import-time switch, NoBornException fallback.
- create-loop-induced-and-noborn-override: TWO post-parse NLO_mode mutation sites — create_loop_induced forces 'noborn' (5392), do_check forces 'all'->'virt' (4413); do_check bracket-pertOrders sub-guards (4421 timing/stab/profile need loops, 4428 check-gauge [[],['QCD']]); 3 [[],['QCD']] sites (4428+4566 live, proc_validity 270-275 dead); bracket-vs-decay user error is proc_validity (loop_interface:245) "ML5 cannot yet decay" (shadows base 3283 "cunjunction" guard); proc_validity 257-275 dead code (if not 'real'). NO Born-vanishing check for explicit [noborn=] — parser trusts the keyword (4868-4869), routes straight to create_loop_induced with no Born gen; Born ABSENCE detected only on the [QCD]/all path via NoBornException (fks/fks_base:181, fks slice). `>` s-channel bracketed form (p p > t t~ > w+ b w- b~ [QCD]) IS parse-accepted (no decay_chains, proc_validity 245 skipped) where the comma form is rejected — probed. LOAD-BEARING: explicit [noborn=QCD] routes master:218-225 -> create_loop_induced BYPASSING aMC@NLO do_add, so C6a:542 squared-order <=-only reject NEVER fires — ^2==/^2> ACCEPTED for [noborn=] (probe g g>z z QED^2==4 [noborn=QCD]) vs REJECTED for [QCD]; noborn procdef-GLOBAL (drops genuine Born, probe b b~>h); has_born-mix reject ORDER-dependent (5408 vs Switcher combine-LO/NLO, FKS-first escapes); decay+^2 rejected 3301-3303.
- nlo-mode-lifecycle-stages: NLO_mode/has_born is a 4-stage lifecycle value — routing classify (extract_process_type 162) -> parse remap (4862-4877) -> stored (5350) -> command override (do_check 4414, create_loop_induced 5392); no single stage authoritative, stages 1/2 disagree by name. Generalizes the switcher/bracket-parse/post-remap-discriminant/create-loop-induced pages; identify the stage before answering "what mode does X become". Only .set('NLO_mode') sites are 4414+5392. perturbation_couplings has a 5th touch-point (amcatnlo do_add 673 overwrite) — see amcatnlo-do_add page.
- amcatnlo-do_add-perturbation-couplings-overwrite: amcatnlo do_add re-parses the bracket a THIRD time (extract_process_type 478), runs bracket-mode/pertOrder-keyed guards (479 compiler skip real/LOonly, 485 NLO-EW banner, 522 "Decay processes cannot be perturbed"), and CONDITIONALLY overwrites perturbation_couplings -> model coupling_orders (673, gated not-orders + nlo_mixed_expansion default True) + bumps orders/squared_orders. Probe: bare [QCD] in loop_sm displays [ all = QCD QED ]; QED=0 [QCD] suppresses it. The bracket-parse probe table is the value at extract_process EXIT, not final. nlo_mixed_expansion is an INTERFACE option (madgraph_interface:3115 default True, `set` 8267, NOT run_card), read at do_add GENERATION time (637 skip-bump/672 overwrite/707 FKS handoff) so set BEFORE generate; pentagon/mixed-loop semantic is OUT-OF-SLICE (fks/madloop via 707).
- np-order-with-bracket-smeftatnlo: NP (perturbative_expansion=0) is NOT bracket-perturbable — only orders with perturbative_expansion>0 (import_ufo:501) enter perturbation_couplings; SMEFTatNLO pert=['QCD'], coupling_orders=['NP','QCD','QED']. END-TO-END [NP]/[QED] are rejected by validate_model (loop_interface:325-326/355-356 "does not allow to generate loop corrections of type") NOT the 5286 guard (5286 fires only on a DIRECT extract_process call — shadowed live; see extract-process-guards-shadowed page); NP==1/NP>1 fail the 4983 constrained-order guard; NP=1/NP<=1 ride as ordinary <= amplitude constraints alongside [QCD]. Explicit NP= SUPPRESSES the amcatnlo overwrite (orders non-empty) and NP is NEVER order-bumped (bump loop iterates only perturbation_couplings); only QCD bumped +1/+2. Bare [QCD] (no orders) in SMEFTatNLO does NOT overwrite-display like loop_sm — it ERRORS at the negative-constraint auto-order guard (amcatnlo:559, qed=-1) before reaching the 672 overwrite; explicit order required. Probed SMEFTatNLO-NLO. ALSO the NP^2<=N squared-order CAP — parsed on extract_process as (value,type), type checked vs _valid_sqso_types ['==','<=','=','>'] (3036), stored sqorders_types{NP:'<='} (5334); NP^2==N parser-ACCEPTED (== valid) but rejected at NLO by C6a amcatnlo:542 (reads sqorders_types), NP^2=N silently rewritten to <= (4936-4939) so it survives; LO↔NLO asymmetry — NP^2==N LO-only, NLO is <=-only-CAP. Different guard/field from the 4983 amplitude-order ==/> reject. C6a IS MODEL-AGNOSTIC — reads sqorders_types with no model/order-name ref, fires on ANY Xxx^2==N/>N on ANY [...] process incl. loop-induced (generate g g > e+ e- mu+ mu- QCD^2==2 [QCD] rejected at 541 BEFORE NoBornException); squared-order signal/bkg/interference isolation at NLO must use cumulative <= runs + subtraction, never a single == bin. Grounded v3.7.1: 541/542, 3036 _valid_sqso_types, 4936-4939 =->rewrite, 5334/5341 store.
- nlo-parse-guard-firing-order: THE ordered catalog of every guard/Exception on the LIVE NLO generate/add parse path, in firing order, each marked LIVE or SHADOWED/DEAD. Stage A Switcher validity (212/269) -> Stage B TWO single-$ guards covering DISJOINT forms (check_add:129 InvalidCmd fires on SPACED `$ g` token; do_add:476 MadGraph5Error fires on GLUED `d$g` — BOTH LIVE, neither shadows the other; 474 "convert" comment IS stale-code-raises; `$$` and `~$g` hit neither) -> Stage C do_add (validate_model 513, decay 522/527, squared-order/auto-order 542/550/559, overwrite 629-674) -> Stage D extract_process inner (gauge pred 4879/raise 4880, constraint pred 4983/raise 4984, tagged 5227, loop-model 5280-5289 shadowed, store 5349/5350). Consolidates the per-mechanism instance pages into one path-ordered verification-at-parse view. Master validity nums: do_add 212, do_check 246, do_generate 269. Probed; B2 corrected DEAD->LIVE.
- extract-process-guards-shadowed-by-outer-routing: extract_process's bracket mode/pert-order guards (4871 invalid-mode, 5269-5273 order_hierarchy, 5280-5289 loop-model) are INNER guards shadowed live by EARLIER outer guards — Switcher _valid_nlo_modes check (268/211, bad OPTION) and aMC@NLO do_add validate_model (513, non-perturbable ORDER); user sees the outer message, inner only on a direct extract_process call. validate_model can also AUTO-UPGRADE sm->loop_sm instead of rejecting. Probe-confirmed (NP/QED/EW/bogus-option/sm-upgrade).
