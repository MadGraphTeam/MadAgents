## Slice
I own EFT computation in MadGraph: EFT-capable UFO models (bundled/online/FeynRules-only), their restrict_*.dat operator selections, EFT power-counting coupling-order constraints (NP/DIM6/EFT, per-insertion value), and documented EFT-NLO specifics + history.
Out of slice: general UFO content (ufo), the restriction algorithm itself (restriction), general coupling-order syntax (coupling-order), NLO mechanics/FKS/MadLoop (NLO slices), amp_split accounting (nlo-export), linear-vs-squared physics judgment (ma-physics), online-fetch mechanics (installation).

## Core operating principles
- Verify against source for THIS input every time; adopt a scope-matching cached wiki page per ma-wiki-as-evidence (sanity-check one cited file:line), else walk source. Pretrained MG recall is a hypothesis.
- Per-insertion EFT increment is model-specific: READ the active model's coupling_orders.py + couplings.py. Never assume (DIM6=+1, NP=+2 are NOT universal).
- Before "which model do I use", ls $MADGRAPH_INSTALL/models/ — confirm what is actually installed.
- Read the active restrict_*.dat: zeroed Wilson coefficients are stripped from the interaction set and drop their order from validation.
- Two-section return (Source-walked facts / Implications); reject unmarked out-of-slice claims in a third section.

## Recent lessons (FIFO, max 5)
- eft-model-install-oscillation + per-insertion-read: distributed EFT models (SMEFTatNLO, SMEFTsim_*, dim6top_*) are MANUALLY-PLACED not bundled — present some installs, absent others (when absent, models/ = stock, e.g. sm/loop_sm/MSSM_SLHA2/taudecay_UFO/hgg_plugin; SMEFTatNLO_running lives under tests/input_files, NOT importable as a models/ model). ALWAYS `ls models/` first; not-on-disk is a correct answer. Per-insertion increment p = READ off the installed model's couplings.py `'NP':N` (or `'DIM6':N`) order dict — NOT expansion_order (that's a CAP: SMEFTsim/dim6top expansion_order=99 but p=1). Anchors seen when present (re-read per installed copy+version, never cache the integer): SMEFTatNLO p=2, SMEFTsim p=1, dim6top DIM6/FCNC p=1. Bins derive from p: σ_int=NP^2==p, σ_quad=NP^2==2p; SMEFTatNLO(even-only)→odd NP^2 empty, SMEFTsim odd allowed. Also: restrict cards OVERRIDE raw UFO parameter defaults → quote OPERATIVE (active-card) value read fresh off coordinates, cache neither. Durable facts on wiki: bundled-eft-models, smeftsim-anchored-taxonomy-and-gaps, smeftatnlo-np-bin-selection-and-odd-empty, smeftatnlo-restrict-card-taxonomy, smeftatnlo-operator-identification. Trigger: any SMEFTatNLO/SMEFTsim/dim6top question.
- loop-sm-modif-external-np: stock loop_sm declares ONLY QCD+QED (coupling_orders.py:9-17, no NP) → `generate ... NP=1` REJECTED verbatim "model order NP not valid for this model (valid one are: QED, QCD, EW, EW^2, aEW, aS)" (probe-confirmed; EW/EW^2/aEW/aS = auto-aliases not extra declared orders). To bin gg→ZZ signal/bkg/interference by NP you need a model that DECLARES NP: `loop_sm_modif` is an EXTERNAL manual-download model (loop_sm_modif.tar.gz off the Offshell_interference wiki), NOT bundled, NOT in _online_model. Its "NP tags HVV / GC_31/GC_32 / p=1" claims are UNVERIFIABLE here — do NOT cache; the p=1 bin map (bkg=NP=0, int=NP^2==1, quad=NP^2==2) is consistent-but-unverified, read the extracted model's coupling_orders.py. Trigger: any loop_sm_modif / gg→ZZ NP-interference / "loop_sm NP=" question.
- nlo-eft-order-rewrite: LO `==` order recipes do NOT transfer to NLO — `NP==k`/`NP^2==2k` both REJECTED (only `<=`); an unset EFT order at NLO defaults to 99 → amp-capped but `NP^2=198` (squared-unbounded), NOT a clean truncation. Trigger: any EFT `[QCD]`/`[QED]` task. Behavior probe-confirmed verbatim; cite line numbers from source, do not trust draft cites (drift of 10-20 lines is common).
- smeftatnlo-restrict-cards: the 4 SMEFTatNLO cards differ in MORE than "coefficient values" — LO→NLO zeroes 5 NLO-incompatible operators (cG,cpG,ctlS3,ctlT3,cblS3) + re-zeroes ONLY the ghost/goldstone widths (ghZ/ghWp/ghWm/G0/G+ → 0); the physical t/Z/W/H widths (DECAY 6/23/24/25) are KEPT, byte-identical to LO (do NOT say NLO re-zeroes t/Z/W/H). no4q zeroes the ENTIRE block DIM64F (every DIM64F coeff, NOT DIM64F4L which is 4-LEPTON; NLO↔no4q diff is DIM64F-only). **DIM64F is "4-fermion all-quark", NOT "4-HEAVY-quark" — it includes LIGHT-quark-coupling ops like cQq83 (=c_Qq^(8,3), GC_35=(cQq81+cQq83)/Λ², attaches t̄tūu/c̄ct̄t/b̄bd̄d/b̄bs̄s, drives qq̄→tt̄ from a light-quark initial state). So under -NLO_no4q, `set cQq83 1.0` is INERT (nonzero→0.000000, `# cQq83` at restrict_NLO_no4q.dat:139 — read zero/nonzero there), σ≈σ_SM_NLO silently; LO keeps it nonzero (restrict_LO.dat:139). Only cQQ8/cQQ1/cQt1/ctt1/cQt8 are truly 4-heavy.** Naming trap: "4-quark" coeffs live in DIM64F. Nonzero coeff values are random placeholders, not physics. Trigger: any SMEFTatNLO restrict-card question — read zero/nonzero status per block, not values; only ghost/goldstone widths flip at NLO; no4q kills light-quark 4-fermion too.
- smeftatnlo-np-bin-empty: SMEFTatNLO NP=+2 per insertion (couplings.py 'NP':2 only, no 'NP':1/':4; coupling_orders.py:9-11 expansion_order=2) ⇒ amplitude NP always EVEN ⇒ squared NP^2 bins all EVEN: σ_int=NP^2==2, σ_quad=NP^2==4, odd bins NP^2==1/==3 IDENTICALLY EMPTY; NP=1 amp bound UNSATISFIABLE → ME pure SM (probe anchor: gg→tt̄ NP=1 = SM-only, ctG invisible; NP=2 adds the ctG interference Δσ≈+14.7pb). SMEFTsim p=1 (when present) shifts int→NP^2==1, quad→NP^2==2 — bin N is NOT portable across EFT UFOs; dim6top DIM6=+1 same rung but order NAME differs (NP^2==N errors on dim6top). Read p off couplings.py per installed copy. Trigger: any "which NP^2 bin / why is my NP^2==1 empty / port SMEFTsim recipe" — read the per-model insertion value first.

## Wiki page index
- bundled-eft-models: roster is SESSION-SPECIFIC — distributed EFT models (SMEFTatNLO/SMEFTsim_*/dim6top_*) are manually-placed not bundled, come and go across installs; `ls models/` every session. Online (_online_model: EWdim6/heft/TopEffTh) vs FeynRules-only; per-insertion increments; taudecay_UFO `EFT` order is NOT dim-6 SMEFT (false positive).
- loop-sm-modif-np-ggzz-interference: gg→ZZ sig/bkg/interference via loop_sm_modif's custom NP order; stock loop_sm declares only QCD+QED (NP rejected, probe-confirmed); loop_sm_modif is EXTERNAL (Offshell_interference wiki tar), not bundled/not _online_model; NP-tags-HVV/p=1 UNVERIFIABLE here.
- dim6top-fcnc-second-eft-order: dim6top declares TWO disjoint EFT orders (DIM6 flavour-conserving + FCNC flavour-changing, both +1, exp_order=99); plus config-flag-conditional QCD order (norm_chromo_gs default False → chromo couplings QCD:0).
- eft-power-counting-parser: extract_process order parsing — amp vs sq orders, == auto-generates NP^2==2*value, = reinterpreted as <=, EW/aEW/aS aliasing; squared-only NP^2==2 infers amp order (LO); [tree=NP]/LoopOption parsing; == LO-only (NLO rejects).
- smeftatnlo-default-restriction-trap: SMEFTatNLO default restriction rejects NP (all coeffs zeroed); use -LO/-NLO; contrast dim6top (no restrict file, works out-of-box).
- smeftatnlo-restrict-card-taxonomy: the 4 cards (default/LO/NLO/NLO_no4q) — 5 DIM6* block taxonomy, which coeffs each zeroes; LO→NLO zeroes cG/cpG/ctlS3/ctlT3/cblS3; no4q zeroes ENTIRE DIM64F block incl. light-quark-coupling cQq83 (→ set cQq83 inert under no4q); DIM64F is "4-fermion all-quark" NOT "4-heavy-quark"; values are random placeholders.
- smeftatnlo-operator-settability-lo-vs-nlo: v1.0.3 settability table keyed on -LO vs -NLO restrict card (grounded restrict lines + `display parameters` probe). cG/cpG/ctlS3/ctlT3/cblS3 settable at LO, zeroed/removed at NLO. CAUTION: cpG NOT available at NLO in v1.0.3 (contra common expectation). cG/ctlS3 inert (0 coupling refs); ctlT3/cblS3 LO-only. Gap: later versions may differ — re-verify installed version.
- smeftatnlo-operator-identification: mapping a physics operator name (e.g. O_lq^(1)) to the correct DIM6* block and parameter — block naming encodes fermion content (DIM64F=all-quark, DIM64F2L=semileptonic, DIM64F4L=4-lepton); verify by vertex legs in vertices.py, never assume block from memory.
- smeftatnlo-np-bin-selection-and-odd-empty: SMEFTatNLO NP=2/insertion → squared NP^2 bins all EVEN (σ_int=NP^2==2, σ_quad=NP^2==4, odd NP^2==1/==3 EMPTY); NP=1 unsatisfiable → SM-only. SMEFTsim NP=1/insertion shifts int→NP^2==1, quad→NP^2==2; bin N not portable across EFT UFOs.
- eft-nlo-specifics: TIR HAS_AN_HEFT_VERTEX flag for CutTools limitation routing; UpdateNotes EFT/SMEFT/HEFT bug history.
- coupling-order-validity-from-surviving-interactions: an order is valid iff a surviving interaction carries it (get_coupling_orders from present interactions, not declared coupling_orders.py); catches any order/any restriction; explains display-vs-generate mismatch.
- eft-expansion-order-and-weighted-default-cap: coupling_orders.py expansion_order silently caps EFT amp order with no constraint (SMEFTatNLO NP=2 auto NP<=2; dim6top DIM6=99 no cap); hierarchy builds WEIGHTED. expansion_order=0 also EXCLUDED by `0<v<99` (base_objects.py:3766) → NO cap, NOT auto-exclude (refutes NPprop=0 doc claim).
- smeftsim-anchored-taxonomy-and-gaps: SMEFTsim_topU3l_MwScheme_UFO (when on disk; session-specific) → NP=1/insertion (couplings.py 'NP':1), coupling_orders zoo (SMHLOOP/NPprop=0/NP exp_order=99 + per-Wilson-coeff NPc<coeff> orders), blocks SMEFT/SMEFTcpv/SMEFTcutoff, restrict names massless/SMlimit_massless — source-verified for topU3l. Bins int=NP^2==1/quad==2. GAP: other flavor/scheme variants, Re/Im spellings, bare-import default restriction.
- eft-rge-running-machinery: EFT operator running (RGE) — UFO all_running_elements hook (import_ufo.py:524), get_running correlated-set grouping (drops aS), conditional RUNNING run_card block (fixed_extra_scale/mue_ref_fixed/mue_over_ref); dormant for bundled SMEFTatNLO/dim6top.
- eft-squared-order-diagram-filtering: LO diagram-survival side of truncation — apply_squared_order_constraints three filters (amp ==/>, positive sq, negative sq); pass_squared_order_constraints sums two amp powers; probe-verified dim6top counts; LO-only (NLO via amp_split).
- eft-nlo-order-determination: aMC@NLO EFT order defaulting — unconstrained NP/DIM6 at NLO → default_unset_couplings (99), amp-capped but sq-unbounded (NOT LO expansion cap); == amp & == sq BOTH rejected at NLO (only <=); nlo_mixed_expansion branches; split_orders forced to all model orders; LO default_unset_couplings!=99 zeroes unmentioned EFT orders.
- eft-order-token-rewriting: GENERALIZATION — the typed EFT order constraint is silently rewritten before enforcement (==→auto-squared, =→<=, squared-only→inferred amp, expansion_order clamp, NLO default 99/198, NLO == rejection, forced split_orders); read the emitted Trying/born process line, never the typed token; re-derive per model.
- eft-model-content-wilson-coupling-vertex: model-content chain — Wilson coeffs (external params in DIM6* lhablocks) → coupling value = coeff/Lambda**2 carrying NP/DIM6 order → effective vertices inherit it; same physical 1/Lambda² insertion = +2 (SMEFTatNLO NP) vs +1 (dim6top DIM6/FCNC), increment is per-model convention.
- eft-model-loop-capability: a UFO is loop-capable iff some order has perturbative_expansion>0 (import_ufo.py:501-508 → LoopModel); SMEFTatNLO perturbation_couplings=['QCD'] only → [QED]/[QCD QED] rejected; user-visible gate is loop_interface.py:356 "cannot handle loop processes" (NOT mg_interface:5286, shadowed); no CT_parameters.py (NP rides CT_couplings order dicts); dim6top tree-only despite CT_couplings.py.
- eft-wilson-coeff-param-card-generation: Wilson-coeff external params (parameters.py lhablock+lhacode+default) → param_card blocks via write_param_card.py ParamCardWriter:63-87; no-restrict (dim6top all coeffs=0.) vs restrict-default (SMEFTatNLO all-zeroed) default-card divergence; block lowercased in card (Block dim6); SAME block name has different lhacode→operator map per model; SMEFTatNLO DIM6 lhacode 1=Lambda(scale) not an operator, 4-fermion blocks default 1.e-10.
