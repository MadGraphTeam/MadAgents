## Slice
I own VEGAS sampling / grid machinery (combine_grid: DiscreteSampler, grid rebinning, xsec/error estimator), run+iteration combination (combine_runs, gen_ximprove combine_grid/combine_iteration 1/sigma^2 + chi2), refine logic (gen_ximprove v4/share/gridpack/nogridupdate, gen_events_security + combining_job), gridpack mode, runtime helicity recycling (get_helicity + hel_recycle.py), HepMC parsing (hepmc_parser.py), run-time HTML cross-section pages (sum_html/gen_crossxhtml), suspect-number/negative-weight/EPS indicators, BIAS scaffolding. NOT mine: phase-space channel construction, launch orchestration, PDF/scale systematics, NLO FKS/virt numerics, detector-level HepMC analysis, BW-window, treatcards. I am the sampling/grid consultant, NOT the numerics-evaluation reviewer.

## Core operating principles
- Verify against source for THIS input every dispatch; in default mode a scope-matching cached page counts as verification (sanity-check one cite). Under mg-deep-verify, walk source every time.
- Hardcoded refine constants (gen_events_security, combining_job, max_event_in_iter, min/max_iter) differ per gen_ximprove subclass — always re-read the chosen class, never assume the base-class value.
- Runtime predictions (warning text, xsec, diagram counts) are HYPOTHESIS until probed; never cache a runtime claim as fact from reading alone.
- EPS/UPS stability numbers exist only for loop-induced/NLO (n_madloop_calls>0); LO tree runs carry none.
- Stay in slice: confirm the sampling/grid/parsing part, name the boundary for phase-space/launch/systematics/NLO/BW territory rather than answering it.

## Recent lessons (FIFO, max 5)
- sigfpe-not-python-combiner + recycler-wraps-at-72: (1) A runtime SIGFPE during integration is NOT combine_grid/DiscreteSampler. combine_grid.py:675/678 (DiscreteSamplerDimension.update, grid_mode!=1 init branch) has UNGUARDED ratio=sum_run/sum_ref & ratio_sqr=sum_run_sqr/sum_ref_sqr — divides by zero if a ref channel's total abs_weight=0 — but Python raises ZeroDivisionError (traceback), NOT a hardware SIGFPE (probe: 1.0/0.0→ZeroDivisionError). SIGFPE is a Fortran/OS trap in the compiled madevent binary (ME eval/genps=phase-space; FP-trap on/off=installation/make_opts), not this combiner. get_new_grid_for_var divides ARE eps-guarded (442 +1e-99, 469 denom=grid-size). (2) HelicityRecycler does NOT bypass wrapping: read_template (hel_recycle.py:724) runs do_multiline (884, char_limit read there) over EVERY substituted sub-line incl helas_calls/jamp/helicity blocks + template scaffold → reflows over-width lines into fixed-form continuation chunks. Cannot emit >132 "Line truncated"; matches (not bypasses) the FortranWriter wrap discipline (file_writers.py:201). A real >132 line is a writer-bypassing hardcoded/hand-Template .f/.inc = OUTPUT slice, not recycler. Trigger: "SIGFPE in VEGAS/grid" or ">132 from recycler/DAG" — refute both.
- discretesampler-tag-case: DiscreteSampler dict key is (name, grid_type) with ORIGINAL-case name ("Helicity"), though read() lowercases lines only to find the marker; bin IDs are strings. Probe-confirmed, don't assume lowercased keys.
- conservative-factor-is-dead: two of my pages CONTRADICTED — combine-grid-vegas said conservative_factor is a DEAD param, gridpack page said "genuinely-used, NOT a dead write." Source settled it: combine_grid.py has only 3 occurrences (sig defaults 164/185, pass-through 177); NO body reads it, twgt=mean/8.0/nb_event hardcodes /8.0. A call site passing conservative_factor=5.0/max_iter (gen_ximprove 644/1759/1774) is the callee DISCARDING it = dead param, not use. Trigger: when two pages disagree dead-vs-live, walk the CALLEE BODY not the call sites; passing an arg ≠ consuming it.
- lhe-spinup-and-event-norm: LHE writer is rw_events.f:225 write_event (fmt 51 = i11,5i5,5e19.11,f3.0,f4.0). SPINUP col13 = real(ic(7)). THREE occurrences of 9 (earlier "9 only Python" was WRONG): (1) EXTERNAL legs status ±1 → MC-SELECTED nhel(i) (unwgt.f:609), NOT 9; (2) RECONSTRUCTED status-2 intermediate resonances → 9 written by FORTRAN, addmothers.f:332 jpart(7,i)=9 "No helicity info for intermediate states", reordered col-7 at 364 → ic(7) → SPINUP (~994/1000 events have a 23 2 ... 9. Z line at √s=M_Z); (3) Python lhe_parser.Particle ctor default 9 (94/117). VTIMUP col12 hardcoded 0. (uniformly 0). event_norm=average(default): every event XWGTUP identical = σ. event_norm default=average; per-event |XWGTUP| set by MultiEventFile.unweight new_wgt (lhe_parser 1224-1239): sum→Σacross/N (Σ=σ,strat3), average→Σacross (avg=σ,strat4), unit→1(±1,strat3), written via copysign (467). TOKEN TRAP: run_card allows 'unity' (banner 4298) but consumer checks 'unit' (lhe_parser 1232/519) — 'unity' matching is unverified/probe. nevents=0→events.lhe deleted, no file, σ still summed (madevent_interface 3909-3911).
- iseed-sentinel-two-paths: iseed reproducibility has TWO distinct paths. LAUNCH path (do_generate_events, madevent_interface 6129-6143): fixed iseed used verbatim for THAT run then reset_iseed_in_run_card REWRITES card to iseed=0 (common_run_interface 4931-4942) → re-run does NOT repeat unless you re-set; iseed=0 CONTINUES from randinit (+3 per run) if present, else randint(1,30107) — NOT a pure fresh draw. GRIDPACK path (run.sh nevents seed [gran] → GridPackCmd 6983): seed is positional arg, written verbatim to randinit, NO auto-reset → directly reproducible per seed; seed=0 ERRORS (7002-7005). Trigger: "is iseed=X reproducible" — pin launch-vs-gridpack first; they differ.

## Wiki page index
- combine-grid-vegas: combine_grid.py — VEGAS grid combination, xsec/error estimator, DiscreteSampler/Bin_Entry format, secondary-unweighting max-wgt, new-grid rebinning.
- iteration-combination-and-results: run/iteration combination — gen_ximprove 1/sigma^2 + chi2 accumulator + instability-security discard; sum_html OneResult/Combine_results, results.dat fields; combine_runs.
- refine-gen-ximprove: gen_ximprove refine — class dispatch (v4/share/gridpack/nogridupdate), gen_events_security + combining_job constants, find/get_job_for_event splitting, get_job_for_precision, share combine_iteration (event carry-over re-unweight + drop-previous-iteration + 0.96×security stop).
- helicity-recycling: runtime helicity recycling — gensym.get_helicity (madevent_forhel threshold at input_app.txt:175, ZEROAMP/Good-Helicity parsing) + HelicityRecycler source-to-source Fortran transform (MathsObject DAG).
- lhe-event-writing-and-normalization: LHE output format — rw_events.f write_event (init/event/particle record layout, SPINUP=selected-helicity not 9), event_norm default average + written_weight per-mode formula (sum/average/unit), LHA strategy 3/4, unity-vs-unit token trap, nevents=0 no-file.
- hepmc-parser: hepmc_parser.py — HepMC2 IO_GenEvent ASCII parsing (E/V/P/N/U/C/H/F), EventFile gz handling, known quirks (helicity=9, comment AttributeError).
- suspect-numbers-and-runstatistics: operational meaning of EPS/UPS/skipped_subchannel/chi2-dof/negative-weight indicators and where each surfaces.
- crossx-html-and-bias: run-time HTML cross-section pages (gen_crossxhtml AllResults/RunResults/OneTagResults, sum_html collect_result) + BIAS module scaffolding pointer.
- biased-event-generation: bias_module — bias MULTIPLIES integrand (auto_dsig_v4.inc:166), divided out at LHE write/parse (XWGTUP de-biased, lhe_parser 3391-3401); impact_xsec = "write bias weight?" (NOT "is σ unbiased"), common/bias member order + positional aliasing, requires_full_event_info early-reconstruction, weighted output, flavour_bias distinct.
- mc-helicity-event-multipliers: MC-over-helicity (nhel==1) inflates per-iteration event budgets by 2**(nexternal//3) (refine max-iter ceiling by 2**(nexternal//2)) — four code sites + probe-verified arithmetic.
- survey-driver-and-splitting: survey driver — gensym.launch (gensym binary → channel list), submit_to_cluster job_strategy dispatch + splitted_grid parallelization, input_app.txt template / gridmode 2 vs -2.
- xsec-combination-ladder: cross-section/error aggregation ladder (PS-point→iteration→channel→job→total) — sum vs average vs 1/sigma^2-weight per rung; xerru always quadrature, xerrc/xsec follow rung rule. Generalizes combine-grid-vegas + iteration-combination-and-results.
- helicity-recycle-fortran-rewrite: HelicityRecycler write side — read_orig line state-machine, reference-counting dead-call elimination, split_amps gauge-dependent CombineAmp/CombineAmpFD/CombineAmpS emission, template substitution + 72-col continuation, symlink zero-ME path. Complements helicity-recycling (discovery+DAG).
- gridpack-readonly-execution: gridpack refine execution — readonly (RO) cwd-redirection of script-write/job-cwd/combine, nprocs onecore vs MultiCore local run, check_events recursive resubmit with events.lhe.previous carry-over, self.gran granularity typo.
- gridpack-creation-and-seed: LO gridpack creation (gridpack=True → warmup survey with hardcoded accuracy/points/iterations read at madevent_interface.py:2578-2581 + create_gridpack tarball) & seed/reproducibility — run.sh arg order (nevents seed [gran]), GridPackCmd seed verbatim→randinit (gridpack needs NONZERO seed, errors on 0), iseed=0 launch-path sentinel (continue-from-randinit else randint) + reset_iseed_in_run_card auto-reset-to-0, NLO seam (no gridpack=True, uses req_acc/MINT → amcatnlo).
