## Slice
I own end-to-end LO `launch`: the flow (do_launch->do_generate_events->run_generate_events), per-stage commands (survey/refine/combine/store/decay), gridpack create+restart, cluster submission (cluster.py backends), treatcards/configure_directory .inc generation, the bin/ launch entry points, and launch-time HTML pages. OUT: phase-space channels, VEGAS/numerical/helicity-recycling internals, card content, NLO launch (amcatnlo), reweight/systematics runtime, downstream-tool internals, auto-width computation.

## Core operating principles
- Verify against source for THIS input every time; adopt a scope-matching cached wiki page per ma-wiki-as-evidence (sanity-check one cited file:line), else walk source.
- Source = code + the config/card files MadGraph reads. A claim about run_card/grid_card/mg5_configuration content is a source claim — read the file.
- Reject unmarked out-of-slice claims; answer only the in-slice portion; name the owning territory for the rest.
- Never cache a runtime prediction as fact from reading alone — mark it hypothesis or probe it.
- Two-section return (Source-walked facts / Implications), concision rule.

## Recent lessons (FIFO, max 5)
- reaching-nevents-target: "fail to reach target" is UNWEIGHTING-side/post-integration (lhe_parser.py:548 max_wgt converged short; :611 all passes short; both in EventFile.unweight, called from do_combine_events 3883/3922 w/ event_target=nevents) -> σ is VALID, only unweighted-event count fell short (efficiency/max-weight problem). do_multi_run (ME 3084) MULTIPLIES: each of N sub-runs = FULL run_card nevents (no /N), total N×nevents, merge.pl-merged, inv-var σ combine; it's the >1M-cap remedy (ME 6479 "Use multi_run for larger statistics"). LO-ONLY (amcatnlo has NO do_multi_run, grep=0). sde_strategy run_card param default at banner 4458 (allowed [1,2], doc-myth says 2 — read fresh; fortran_name sde_strat->run.inc; semantics=phase-space). Trigger: "fail to reach target / not enough events / multi_run / how to get more stats / sde_strategy".
- runcard-scan-orchestration: run_card `scan:[...]=name` = LO scan. Registered in scan_set at __setitem__ (banner 1122, gated allow_scan). Loop entry: run_generate_events (ME 2564) @scanparamcardhandling(run_card_scan=True); decorator new_fct CR 7980 `if run_card_scan`->scan_over_run_card CR 7904 (ONE run_generate_events call, loops same obj, no per-point subprocess/pool). RunCardIterator.iterate (banner 6450): eval(def_list) (6468, any list-expr); itertools.product(*lengths) (6483) per KEY(scan id) -> bare `scan:` = distinct neg key each = CARTESIAN product; same `scanN:` id = LOCKSTEP (shared pos 6487-91). NAMING CORRECTED: points = run_01_01,run_01_02,... via get_next_name (banner 6572, "_00" appended->always digit->`_scan_02` fallback 6580 UNREACHABLE); doc's run_01_scan_02 WRONG for run_card. Summary = Events/scan_<getscanname>.txt (CR 7847/7951) + per-point Events/<run>/params.dat (6563). nevents per point (full body); seed differs only if iseed=0; NO grid reuse across points (fresh survey/refine each). THREAD-LEAK doc claim is STALE/FIXED in 3.7.1: wait() sets stoprequest when keep_thread=False (cluster 964, default 672)->worker loop exits (717)+self-removes (787); MultiCore reused unless nb_core changes (CR 3687-92). Trigger: any parameter-scan / scan: / thread-leak / "N launches crash" question.
- run_mode-not-a-card-param: run_mode/nb_core are mg5 OPTIONS (defaults at CR 686/691; run_mode read fresh, nb_core None=auto-all-cores), NOT run_card params. In-dialog AskforEditCard.do_set (CR 5868) has NO run_mode branch + no mother delegation -> `set run_mode` typed inside the launch block emits `invalid set command` (CR 6182/6359) and no-ops. Set them at mg5/shell level BEFORE launch (CommonRunCmd.do_set CR 3571). Gridpack tarball = madevent/ + run.sh at ROOT (make_gridpack); use `./run.sh <nev> <seed>` from extraction root, NOT `cd madevent`; LHE -> Events/GridRun_<seed>/ then moved to events.lhe.gz. Banner = <run>_<tag>_banner.txt. Trigger: scripted/.mg5 launch, "why didn't set run_mode work", gridpack-usage questions.
- nevents0-integration-only: nevents=0 is NOT survey-only — survey AND refine both run (refine 0 invoked, ME 2601/2618), but combine_events discards every G-dir events.lhe via `if gridpack or nevents==0: remove+continue` (ME 3909-3911, mirror 3975), so len(AllEvent)==0 -> nb_event=0, NO unweighted_events.lhe written; xsec IS still computed+reported. Defaults: nevents (banner 4214), iseed (4215; 0=auto-seed sentinel = randinit-else-randint, ME 6136-6143) — read values fresh at coordinates. Trigger: any nevents/iseed default or "integration-only / nevents=0" question. Don't say "survey only" or "refine skipped".
- restart_gridpack-precision-crash: `restart_gridpack --precision=<v>` raises TypeError (line[s:].split(1)[0], madevent_interface 2353) in Py3; only the default min_precision path (no flag) works. Trigger: any user asking to restart a gridpack at a precision threshold. Don't soft-pedal a parse bug as "bug-prone" — probe it; it's a hard crash.

## Wiki page index
- reaching-nevents-target: "fail to reach target" = UNWEIGHTING failure post-integration (lhe_parser.py:548/611 in EventFile.unweight, called from do_combine_events) — σ VALID, only event count short; do_multi_run MULTIPLIES stats (N×nevents, NOT nevents/N; LO-only, absent in amcatnlo; remedy for 1M cap); low-eff channel warning gen_ximprove:1612; sde_strategy run_card param default at banner.py:4458 (fortran_name sde_strat -> run.inc, semantics=phase-space).
- runcard-scan-orchestration: run_card `scan:` — detection (scan_set, allow_scan), RunCardIterator eval+itertools.product loop (bare scan:=product, scanN:=lockstep), naming run_NN_01.. (NOT _scan_02), Events/scan_*.txt summary + params.dat, seed/nevents/no-grid-reuse-per-point, v3.7.1 MultiCore thread lifecycle (leak is FIXED: wait sets stoprequest, MultiCore reused).
- bias-module-wiring: biased event generation — bias_module/bias_parameters RunCardLO-only (NLO=flavour_bias); registration/defaults, `{k:v}=bias_parameters` dict card syntax, treatcards param-reconcile + BIAS/bias.inc Fortran injection, configure_directory copy/validate/compile+link, None->dummy no-op always linked, correct_bias at combine.
- scripted-execution-and-config-options: non-interactive .mg5 launch — run_mode default=2/nb_core=None are mg5-OPTIONS not card params (set BEFORE launch; `set run_mode` inside dialog = "invalid set command", CR 6182); card-path drop auto-detects by CONTENT + copies (copy_file CR 7693, default 7220); EOF/done/0 terminators; banner=<run>_<tag>_banner.txt; gridpack run.sh usage.
- launch-flow-orchestration: do_launch->do_generate_events->run_generate_events stage sequence, gridpack-warmup branch, zero-xsec handling.
- survey-refine-and-thresholds: do_survey/do_refine internals, difficult-integration trigger (mmjj/hard_survey), second-refine threshold bypass via prev_cross, run_config.inc rewrite.
- gridpack-create-and-restart: gridpack warmup opts, do_create_gridpack (store4grid/make_gridpack, GridRun toggle), do_restart_gridpack channel resubmit.
- treatcards-and-configure-directory: .inc generation, LO vs shared common_run treatcards split, ninitial==1 beam zeroing, lhapdf/bias compile setup, mtime early-out.
- cluster-submission-backends: Cluster base + MultiCore thread-queue backend (worker/queue/GPU round-robin, packet chaining, fail-fast on non-[0,143,-15], __debug__ re-raise), from_name registry + PBS specifics (maximum_submited_jobs throttle, cluster.py:52), multiple_try linear-backoff retry (misc.py:452), run_mode/cluster_type keys, need_transfer/submit2 staging, modify_interface no-op base.
- launch-entrypoints-and-html: bin/generate_events + MELauncher flag mapping, make_all_html_results, AllResults.add_detail prev_cross, results.html/index.html, do_banner_run, do_open (`open index.html`->PROC_DIR root; check_open me_dir/Cards/HTML resolution order).
- nevents-iseed-and-zero-events: nevents/iseed defaults (banner.py:4214/4215; iseed 0=auto-seed sentinel = randinit-else-randint) read fresh; nevents==0 = survey+refine run but combine_events discards all G-dir events (ME 3909) -> no LHE, xsec still computed.
- launch-time-runcard-overrides: cross-cutting lens — launch-time silent run_card/effort overrides (gridpack opts, mmjj/hard_survey boost, second-refine skip, loop-induced run_mode force, ninitial==1 beam zero, bias_parameters discard, 1M cap, mtime early-out); answers "why did my run_card setting not take effect".
- combine-store-compile-stages: do_combine_events(+_partial)/do_combine_iteration/do_store_events/do_compile — G-dir results -> unweighted LHE, banner-at-combine, chunked unweight, low-event warning, ftn25/log cleanup.
- lhe-header-banner-assembly: how the run banner BECOMES the LHE <header> — Banner(dict) ordered-tag write (banner.py:387, banner_header.txt "reproduce the events" template verbatim), tags MGRunCard/slha(param)/MG5ProcCard(model in proc_card, no <model> tag)/mgversion/MGGenerationInfo(=nevents REQUEST not yield); DOUBLE write: standalone <run>_<tag>_banner.txt (close_tag=True) + inline LHE header via unweight->banner.write(close_tag=False) lhe_parser.py:559 with init-cross + lha_strategy(±3 unit/sum,±4 avg) + unweight-tag mutation.
- monitor-status-and-shell-results: run-monitoring/status layer — monitor() cluster-wait + error prompt, update_status() HTML-write throttle (interval at CR 3968-3972), GridPackCmd silent override (cluster_mode=0 forced), add_error_log_in_html debug.log, keep_cards dot-hide, print_results_in_shell (Width vs Cross-section by ninitial), store_result to_store archiver.
- launch-menu-switcher: AskRun ControlSwitch + ask_run_configuration — the exact 5-switch set + verbatim option strings (shower Pythia6|Pythia8|OFF, detector PGS|Delphes|OFF, analysis ExRoot|MA4|MA5|Rivet|OFF, madspin OFF|ON|onshell|full, reweight OFF|ON), module-availability gating (check_available_module), card-presence-driven default selection, cards-list construction feeding keep_cards, silent consistency auto-changes (Py8+PGS->detector OFF, Rivet->shower Py8), madspin spinmode shortcuts, -f skips card prompt.
- gridpack-lifecycle-two-phase-degradation: generalization — gridpack is a CLASS substitution + 2-phase degradation (Phase A warmup in outer MadEventCmd special-cases integration; Phase B event-gen in GridPackCmd no-ops update_status + forces cluster_mode=0). Locates the whole "why did my gridpack run go silent/single-core/different-accuracy/no-refine/no-unweighting" class. Probe-verified this session.
- event-postprocessing-stages: launch-time post-unweighting event-modification hooks — boost_events (run_card boost_event lambda, eval'd, online-disallowed, in-place LHE boost-to-rest-frame) and add_time_of_flight (exponential proper-decay-length sampling vtim=c*expovariate(width/hbar)); guards + order in run_generate_events (boost before reweight/decay, ToF after decay).
- run-management-commands: user-facing run/config mgmt OUTSIDE the launch flow — do_remove (level-filtered deletion, banner-protected, remove-all refuses if run named 'all'), do_save (persist changed options to process-local me5_configuration; core-mg opts dropped unless --all), do_edit_cards (reuses ask_run_configuration menu), do_print_results (reprint stored OneTagResults).
