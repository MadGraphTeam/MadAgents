Your previous output had the following issues:

{error_list}

Please write a corrected file to `{output_path}` with exactly {num_questions} questions. Each question must be a JSON object with "text" and "reference_answer" keys.
