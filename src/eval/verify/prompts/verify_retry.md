The file `{verdicts_path}` has validation issues:

{error_list}

Please edit the file to fix these issues. Each object must have: `claim` (string), `correct` (true/false/null), `method` (string or null), `evidence` (list of strings), and `explanation` (non-empty string).
