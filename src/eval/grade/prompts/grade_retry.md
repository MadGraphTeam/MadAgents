Your previous output had the following issues:

{error_list}

Please write a corrected file to `{output_path}`. The file must contain a JSON object with `grade` (string), `tags` (list of strings), and `explanation` (string). The `grade` must be one of the valid grade names. Each tag must be one of the valid tag names. Tags can be an empty list.
