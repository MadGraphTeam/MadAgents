Your previous output had the following issues:

{error_list}

Please write a corrected file to `{output_path}`. The file must be a JSON object with keys `doc_gap`, `doc_incorrect`, and `doc_ambiguous`, each containing a list. Each finding must have `problem`, `correct_info`, and `recommendation` fields. Empty lists are acceptable.
